node_modules/
dist/

# Regenerated by .claude/hooks/session-start.sh from skills-lock.json /
# package.json's pinned `skills` version — not committed, just like
# node_modules.
.agents/
.claude/skills/

# Per-machine session state, not shared config.
.claude/settings.local.json

.env
