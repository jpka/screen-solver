# No exercise on screen

The window shows only the site header — a kata title and byline, a "confirm your email" banner, and the language and runtime selectors — with the problem statement, editor and tests all off screen.
